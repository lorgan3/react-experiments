import * as React from 'react';
import TopologyArea from '../topology/TopologyArea';

export const Example: React.StatelessComponent<{}> = () => {
    return (
        <TopologyArea sort={false} />
    );
};
